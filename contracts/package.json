{
    "name": "face-id-verification-contracts",
    "version": "1.0.0",
    "description": "Smart contracts for face ID verification",
    "main": "index.js",
    "scripts": {
        "test": "hardhat test",
        "compile": "hardhat compile",
        "deploy": "hardhat run scripts/deploy.js --network localhost"
    },
    "dependencies": {
        "dotenv": "^16.0.2",
        "hardhat": "^2.11.1"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^5.0.0",
        "@openzeppelin/contracts": "^5.2.0"
    }
}
